package edu.usfca.cs272;

/**
 * Processes search queries and calculates the search results and their scores
 *
 * @author Matthew Kahane
 */
public class WordSearcher {

	// TODO parse, clean, remove duplicates, and sort the search query

	// TODO generate search results from the above output
	// (look for those words in the wordIndex, path is result,
	// positions/count will be used later)

	// TODO Calculate the search results. Should be sorted by score, raw count of matches,
	// then alphabetical location.
	//  Score = total words in the file / total matches for any of the query stems
	// must be different for partial vs exact
}
